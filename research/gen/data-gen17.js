/* Worker 17 — neue FOs aus 280er-Liste. NUR diese Datei bearbeiten. Schema: research/SHARED_INSTRUCTIONS.md
 *
 * STATUS 2026-07-24: KEINE Firmen eingetragen — Web-Recherche in dieser Session nicht möglich.
 *   - WebSearch-Budget erschöpft (200/200 fleetweit verbraucht).
 *   - Externe HTTPS-Egress-Policy blockt ALLE Hosts (Gateway antwortet 403 auf CONNECT;
 *     bestätigt via /__agentproxy/status und weil selbst example.com / wikipedia.org 403 liefern).
 *   - Firmensites (laborgh.de, gertler-estates.de ...), Suchmaschinen, Northdata, IZ etc. alle 403.
 * Da laut SHARED_INSTRUCTIONS §3/§0 nichts erfunden werden darf (aum/reQuote/confirmedObjects
 * brauchen echte Quellen; §0-FO-Gate braucht Verifikation) und keine Quelle ladbar war, wurde
 * bewusst KEIN memory-only Datensatz erzeugt. Slice-17 (11 Firmen) muss mit wiederhergestelltem
 * Web-Zugriff erneut bearbeitet werden. Datei bleibt valider, leerer, malformed-freier Stub.
 */
window.FO_DATA = (window.FO_DATA || []).concat([
]);
